# ============================================================================
# FICHIER .env POUR DÉVELOPPEMENT LOCAL
# ============================================================================
# Copie rapide pour démarrer en développement
# 
# INSTRUCTIONS :
# 1. Copier ce fichier vers .env : cp .env.local.example .env
# 2. Le fichier .env ne sera JAMAIS commité (.gitignore)
# 3. Pour la production, utiliser .env.example comme base
# ============================================================================

# ----------------------------------------------------------------------------
# SERVICE D'EXTRACTION SAR - DÉVELOPPEMENT LOCAL
# ----------------------------------------------------------------------------

SAR_EXTRACTION_PORT=5001
SAR_EXTRACTION_HOST=0.0.0.0
SAR_EXTRACTION_DEBUG=True
SAR_EXTRACTION_PUBLIC_URL=http://localhost:5001

# ----------------------------------------------------------------------------
# WEBHOOKS N8N - DÉVELOPPEMENT (optionnel)
# ----------------------------------------------------------------------------

# Laisser vides pour désactiver les webhooks en dev
N8N_WEBHOOK_ARCHIVE_URL=
N8N_WEBHOOK_ARCHIVE_SECRET=

N8N_WEBHOOK_SAR_ADDRESS_URL=
N8N_WEBHOOK_SAR_SECRET=

# ----------------------------------------------------------------------------
# CORS - DÉVELOPPEMENT LOCAL
# ----------------------------------------------------------------------------

# Autoriser localhost pour les tests
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://localhost:3000

# ----------------------------------------------------------------------------
# LIMITES - DÉVELOPPEMENT
# ----------------------------------------------------------------------------

MAX_UPLOAD_SIZE_MB=50
EXTRACTION_TIMEOUT_SECONDS=60

# ----------------------------------------------------------------------------
# LOGS - DÉVELOPPEMENT
# ----------------------------------------------------------------------------

LOG_LEVEL=DEBUG
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# ============================================================================
# NOTES
# ============================================================================
#
# ✅ Ce fichier est SAFE pour les tests locaux
# ⚠️  Pour la production, générer de vrais secrets :
#    - openssl rand -base64 32
#    - python -c "import secrets; print(secrets.token_urlsafe(32))"
# 🔒 Les webhooks peuvent rester vides en dev (fonctionnalité désactivée)
#
# ============================================================================
