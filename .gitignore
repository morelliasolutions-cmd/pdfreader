# Environment variables
.env
.env.local
.env.*.local

# Node modules
node_modules/

# Logs
*.log
sync.log

# OS files
.DS_Store
Thumbs.db

# Easypanel API configuration (contains sensitive keys)
config/easypanel.json

# Supabase deployment configuration (contains secrets)
config/supabase-deployment.json

# SSH configuration (contains credentials)
config/ssh.json
ssh-config.txt
ssh-credentials.json
# Migration configuration (contains cloud credentials)
.env.migration
*.migration.env

# Local Supabase env file (contains sensitive passwords)
config/supabase.env.local
